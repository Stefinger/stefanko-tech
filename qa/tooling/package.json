{
  "name": "stefanko-qa-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Local-only QA tooling. Deliberately OUTSIDE the production dependency graph so Railpack never provisions Chromium/FFmpeg system packages for the Railway build.",
  "type": "module",
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "puppeteer": "^25.3.0"
  }
}
